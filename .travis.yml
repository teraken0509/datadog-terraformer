dist: trusty
sudo: required
services:
- docker
language: go
go:
- 1.11.1

env:
  - GO111MODULE=on

install:
  - go mod download

script:
  - make fmtcheck
  - make errcheck

after_success:
  - test -n "$TRAVIS_TAG" && curl -sL http://git.io/goreleaser | VERSION=v0.20.0 bash